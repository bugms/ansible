---
- name: Atualizar cache do APT nos servidores Debian
  hosts: debian_servers
  become: yes

  tasks:
    - name: Atualizar cache do APT
      apt:
        update_cache: yes
        cache_valid_time: 3600
